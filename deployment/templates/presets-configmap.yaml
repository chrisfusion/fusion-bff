apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "fusion-bff.fullname" . }}-presets
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "fusion-bff.labels" . | nindent 4 }}
data:
  presets.yaml: |
    {{- .Files.Get "presets.yaml" | nindent 4 }}
