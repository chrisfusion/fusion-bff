apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: fusion-bff
  namespace: dev-fusion
spec:
  interval: 5m
  chart:
    spec:
      chart: fusion-bff
      version: ">=0.1.0-0"
      sourceRef:
        kind: HelmRepository
        name: fusion-bff
        namespace: flux-system
  values:
    replicaCount: 1
    config:
      oidcIssuerUrl: "https://keycloak.dev.internal/realms/fusion"
      oidcClientId: "fusion-gui-dev"
      allowedUsers: ""
      forgeUrl: "http://fusion-forge.dev-fusion.svc.cluster.local:8080"
      indexUrl: "http://fusion-index-backend.dev-fusion.svc.cluster.local:8080"
      weaveUrl: "http://fusion-weave-api.dev-fusion.svc.cluster.local:8082"
